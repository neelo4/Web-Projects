{isPolicyEntitlementView && canAmend && !futureTermEffectiveDate && (
  <ModalButton
    modalHeading={tt('constants.unavailableAssetChangeModal.title')}
    modalContent={
      tExists('pages.callUs.showNumberText') ? (
        <ShowPhoneNumber
          maskedNumber={tt('pages.callUs.maskLinkLabel')}
          unmaskedNumber={tt('constants.phoneNumbers.generalEnquiries')}
          tt={tt}
          prefix={tt('constants.unavailableAssetChangeModal.descriptionPrefix', {
            policyNumber,
          })}
          suffix={tt('constants.unavailableAssetChangeModal.descriptionSuffix', {
            policyNumber,
          })}
        />
      ) : (
        t('constants.unavailableAssetChangeModal.description', {
          policyNumber,
        })
      )
    }
    policyNumber={policyNumber}
    name={tt(`editButton.vehicleUsage.${assetType}`)}
  >
    <Icon className="i-phone pv-mr-4" />
    {tt(`editButton.vehicleUsage.${assetType}`)}
  </ModalButton>
)}
