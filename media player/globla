import { render } from '@testing-library/react';
import { GlobalEventsProvider } from '@iag-common/mfe-global-events-context';
import { Brand } from '../../models/Brand';
import { HostPlatform } from '../../models/Platform';

// Add this constant at the top of your test file
const GLOBAL_EVENTS_PROPS = {
  moduleName: 'policy-correspondence-mfe',
  componentName: 'routes-and-summary',
  moduleVersion: '1.0.0',
  pageName: 'policy-correspondence'
};

describe('PolicyCorrespondence', () => {
  const setup = async (brand: Brand, partyId = partyId1, platform = HostPlatform.ONLINE_ACCOUNTS): Promise<void> => {
    render(
      <GlobalEventsProvider {...GLOBAL_EVENTS_PROPS}>
        <I18nextProvider i18n={(await getI18next(brand))}>
          <Router>
            <AppContextProvider partyId={partyId} isConsultant={platform !== HostPlatform.ONLINE_ACCOUNTS}>
              {/* Your existing component content */}
            </AppContextProvider>
          </Router>
        </I18nextProvider>
      </GlobalEventsProvider>
    );
  };

  // Rest of your test cases remain the same
});
